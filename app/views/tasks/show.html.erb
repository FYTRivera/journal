<%= render "layouts/header"%>
<div class="wrapper">
  <div class="card">
    <%= render @task %>

    <div>
      
      <%= link_to "Edit this task", edit_category_task_path(@category, @task) %> |
      <%= link_to "Back to tasks", category_tasks_path(@category) %>

      <%= button_to 'Destroy Task', [@category, @task], method: :delete %>
    </div>
  </div>
</div>
